version: '3.8'

services:
  flask:
    build: 
      context: ./
      dockerfile: Dockerfile
    ports: 
      - "5000"
  nginx:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - flask
    ports:
      - "5000:80"
